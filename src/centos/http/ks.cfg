# Kickstart file automatically generated by anaconda.

#version=DEVEL
install
cdrom
lang en_US.UTF-8
keyboard us
network --onboot no --device eth0 --bootproto dhcp --noipv6
root pw --iscrypted $6$0wGXpNTXjYmoEusc$Iv6PEC468.cyyCLdP.6MUP.C3DzgYqisI6qyGeumkAKg0UGYEpaAURqx7EubHc3n9TmPPG8nDnEQYms1IZd31
firewall --service=ssh
authconfig --enableshadow --passalgo=sha512
selinux --enforcing
timezone --utc America/Chicago
bootloader --location=mbr --driveorder=sda --append="nomodeset crashkernel=auto rhgb quiet"
# The following is the partition information you requested
# Note that any partitions you deleted are not expresed
# here so unless you clear all partitions first, this is
# not guaranteed to work
#clearpart --all --drives=sda

#part /boot --fstype=ext4 --size=500
#part pv.008002 --grow --size=1

#volgroup vg_vagrant --pesize=4096 pv.008002
#logvol / --fstype=ext4 --name=lv_root --vgname=vg_vagrant --grow --size=1024 --maxsize=51200
#logvol swap --name=lv_swap --vgname=vg_vagrant --grow --size=2048 --maxsize=2048

repo --name="CentOS" --basecurl=cdrom:sr0 --cost=100

%packages --nobase
@core
%end

# Appended for Vagrant
%post
wget -O/etc/pki/tls/certs/ca-bundle.crt http://curl.haxx.se/ca/cacert.pem
groupadd vagrant -g 500
useradd vagrant -g vagrant -G wheel -u 500
echo "vagrant" | passwd --stdin vagrant
#echo "vagrant        ALL=(ALL)       NOPASSWD: ALL" >> /etc/sudoers
sed -i "s/^.*requiretty/#Defaults requiretty/" /etc/sudoers
